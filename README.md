# منصة سَهول - الواجهات المحسّنة

## نظرة عامة

منصة **سَهول** هي نظام إدارة زراعية ذكية متكامل يوفر أدوات متقدمة لإدارة المزارع والمعدات والعمليات الزراعية. تم تطويرها باستخدام أحدث التقنيات لتوفير تجربة مستخدم سلسة وفعالة.

## التقنيات المستخدمة

### Frontend
- **React 19**: مكتبة واجهة المستخدم
- **TypeScript**: لغة البرمجة المكتوبة
- **Tailwind CSS 4**: إطار عمل التصميم
- **shadcn/ui**: مكونات واجهة المستخدم
- **tRPC**: للاتصال بين العميل والخادم
- **Wouter**: للتوجيه
- **Google Maps API**: للخرائط والمواقع

### Backend
- **Node.js**: بيئة التشغيل
- **Express**: إطار عمل الخادم
- **tRPC**: للـ API المكتوب
- **Drizzle ORM**: للتعامل مع قاعدة البيانات
- **MySQL/TiDB**: قاعدة البيانات

### المصادقة
- **Manus OAuth**: نظام المصادقة المدمج
- **JWT**: للجلسات الآمنة

## الميزات الرئيسية

### 1. لوحة التحكم الرئيسية
- نظرة عامة شاملة على المزرعة
- مؤشرات الأداء الرئيسية (KPIs)
- إحصائيات في الوقت الفعلي
- تنبيهات وإشعارات فورية

### 2. إدارة المزارع
- إضافة وتحرير المزارع
- تتبع المساحات والمواقع
- إدارة الحقول والمحاصيل
- معلومات تفصيلية لكل مزرعة

### 3. إدارة المعدات
- تسجيل جميع المعدات الزراعية
- تتبع حالة المعدات (نشط، صيانة، خامل، متقاعد)
- سجل ساعات التشغيل
- معلومات الصيانة والتكاليف

### 4. مخطط العمل (Work Planner)
- إنشاء خطط عمل موسمية
- جدولة المهام والأنشطة
- تعيين المهام للمشغلين
- تتبع التقدم والإنجاز
- حساب التكاليف المقدرة والفعلية

### 5. المراقبة الحية (Live Monitoring)
- تتبع موقع المعدات في الوقت الفعلي
- خريطة تفاعلية مع علامات GPS
- مراقبة السرعة ومستوى الوقود
- درجة حرارة المحرك
- تحديثات تلقائية كل 30 ثانية

### 6. نظام التنبيهات
- تنبيهات مخصصة حسب الأولوية
- إشعارات للحالات الحرجة
- نظام الإقرار والمتابعة
- سجل كامل للتنبيهات

### 7. نظام إدارة المستخدمين
- 4 أدوار مختلفة (مدير، مدير، مشغل، مزارع)
- صلاحيات متقدمة لكل دور
- إدارة كاملة للمستخدمين

## البنية التقنية

### قاعدة البيانات

#### الجداول الرئيسية:
- `users`: المستخدمون والأدوار
- `farms`: المزارع
- `fields`: الحقول
- `equipment`: المعدات
- `workPlans`: خطط العمل
- `tasks`: المهام
- `alerts`: التنبيهات

### API Endpoints

جميع الـ endpoints متاحة عبر tRPC:

```typescript
// المستخدمون
trpc.users.list.useQuery()
trpc.users.updateRole.useMutation()

// المزارع
trpc.farms.list.useQuery()
trpc.farms.create.useMutation()

// الحقول
trpc.fields.list.useQuery()
trpc.fields.create.useMutation()

// المعدات
trpc.equipment.list.useQuery()
trpc.equipment.create.useMutation()

// خطط العمل
trpc.workPlans.list.useQuery()
trpc.workPlans.create.useMutation()

// المهام
trpc.tasks.list.useQuery()
trpc.tasks.create.useMutation()
trpc.tasks.updateStatus.useMutation()

// التنبيهات
trpc.alerts.list.useQuery()
trpc.alerts.create.useMutation()
trpc.alerts.acknowledge.useMutation()
```

## التثبيت والتشغيل

### المتطلبات
- Node.js 22+
- MySQL/TiDB
- pnpm

### خطوات التثبيت

1. **تثبيت الاعتماديات**
```bash
pnpm install
```

2. **إعداد قاعدة البيانات**
```bash
pnpm db:push
```

3. **تشغيل الخادم**
```bash
pnpm dev
```

4. **الوصول إلى التطبيق**
افتح المتصفح على: `http://localhost:3000`

## البنية الهيكلية

```
sahool_ui_enhanced/
├── client/                 # التطبيق الأمامي
│   ├── src/
│   │   ├── pages/         # صفحات التطبيق
│   │   │   ├── Home.tsx
│   │   │   ├── Dashboard.tsx
│   │   │   ├── Farms.tsx
│   │   │   ├── Equipment.tsx
│   │   │   ├── WorkPlanner.tsx
│   │   │   ├── LiveMonitoring.tsx
│   │   │   └── Alerts.tsx
│   │   ├── components/    # المكونات المشتركة
│   │   │   ├── ui/       # مكونات shadcn/ui
│   │   │   ├── DashboardLayout.tsx
│   │   │   └── Map.tsx
│   │   ├── lib/          # المكتبات المساعدة
│   │   └── index.css     # التصميم العام
│   └── public/           # الملفات الثابتة
├── server/               # الخادم
│   ├── db.ts            # دوال قاعدة البيانات
│   ├── routers.ts       # tRPC routers
│   └── _core/           # الملفات الأساسية
├── drizzle/             # Schema وMigrations
│   └── schema.ts
└── shared/              # الملفات المشتركة
```

## التصميم

### نظام الألوان
- **اللون الأساسي**: أخضر (يناسب الطابع الزراعي)
- **الخلفية**: أبيض/رمادي فاتح
- **النصوص**: رمادي داكن
- **التنبيهات**: أحمر، أصفر، أزرق، أخضر

### الخطوط
- نظام الخطوط الافتراضي للمتصفح
- دعم كامل للعربية

### الاستجابة
- تصميم متجاوب بالكامل
- يعمل على جميع الأجهزة (هاتف، تابلت، سطح المكتب)

## الأمان

- مصادقة OAuth آمنة
- جلسات JWT مشفرة
- حماية CSRF
- التحقق من الصلاحيات على مستوى API
- تشفير البيانات الحساسة

## الأداء

- تحميل سريع للصفحات
- تحديثات تفاعلية
- تخزين مؤقت ذكي
- استعلامات قاعدة بيانات محسّنة

## الاختبارات

```bash
# تشغيل الاختبارات
pnpm test

# فحص TypeScript
pnpm type-check

# فحص الأكواد
pnpm lint
```

## النشر

```bash
# بناء التطبيق للإنتاج
pnpm build

# تشغيل النسخة المبنية
pnpm start
```

## المساهمة

نرحب بالمساهمات! يرجى:
1. Fork المشروع
2. إنشاء فرع للميزة الجديدة
3. Commit التغييرات
4. Push للفرع
5. فتح Pull Request

## الترخيص

جميع الحقوق محفوظة © 2025 منصة سَهول

## الدعم

للدعم والاستفسارات، يرجى التواصل عبر:
- البريد الإلكتروني: support@sahool.com
- الموقع: https://sahool.com

---

**منصة سَهول - زراعة ذكية، مستقبل مستدام** 🌱
